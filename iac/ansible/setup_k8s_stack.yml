# iac/ansible/setup_k8s_stack.yml
---
- name: Provision MLOps & Observability Stack
  hosts: all
  gather_facts: no
  connection: local

  vars:
    knative_version: v1.10.0
    kserve_version: v0.11.2 
    istio_namespace: istio-system

  tasks:
    - name: List all namespaces
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_path }}"
        kind: Namespace
      register: ns_list

    - debug:
        msg: "{{ ns_list.resources | map(attribute='metadata.name') | list }}"

    - name: Add Helm Repositories
      kubernetes.core.helm_repository:
        name: "{{ item.name }}"
        repo_url: "{{ item.url }}"
        state: present
      loop:
        - { name: istio, url: https://istio-release.storage.googleapis.com/charts }
        - { name: prometheus-community, url: https://prometheus-community.github.io/helm-charts }
        - { name: grafana, url: https://grafana.github.io/helm-charts }
        - { name: community-charts, url: https://community-charts.github.io/helm-charts }

    - name: Install Istio Base & Control Plane
      kubernetes.core.helm:
        kubeconfig: "{{ kubeconfig_path }}"
        name: "{{ item.name }}"
        chart_ref: "istio/{{ item.chart }}"
        release_namespace: "{{ istio_namespace }}"
        create_namespace: true
        wait: true
      loop:
        - { name: istio-base, chart: base }
        - { name: istiod, chart: istiod }

    - name: Install Knative Serving
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        src: "{{ item }}"
        wait: true
      loop:
        - "https://github.com/knative/serving/releases/download/knative-{{ knative_version }}/serving-crds.yaml"
        - "https://github.com/knative/serving/releases/download/knative-{{ knative_version }}/serving-core.yaml"
        - "https://github.com/knative/net-istio/releases/download/knative-{{ knative_version }}/net-istio.yaml"

    - name: Ensure kserve namespace exists
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Namespace
          metadata:
            name: kserve

    - name: Remove old/broken KServe CRDs
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: absent
        kind: CustomResourceDefinition
        name: "{{ item }}"
      loop:
        - inferenceservices.serving.kserve.io
        - trainedmodels.serving.kserve.io
        - clusterservingruntimes.serving.kserve.io
        - servingruntimes.serving.kserve.io
      ignore_errors: true

    - name: Install KServe CRDs (Official Bundle)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        src: "https://github.com/kserve/kserve/releases/download/{{ kserve_version }}/kserve-crd.yaml"
        wait: true

    - name: Install KServe Core (Official Bundle)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        src: "https://github.com/kserve/kserve/releases/download/{{ kserve_version }}/kserve.yaml"
        wait: true

    - name: Install kube-prometheus-stack
      kubernetes.core.helm:
        kubeconfig: "{{ kubeconfig_path }}"
        name: monitoring
        chart_ref: prometheus-community/kube-prometheus-stack
        release_namespace: observability
        create_namespace: true
        wait: true

    - name: Install Loki
      kubernetes.core.helm:
        kubeconfig: "{{ kubeconfig_path }}"
        name: loki
        chart_ref: grafana/loki
        release_namespace: observability
        values:
          grafana:
            enabled: false

    - name: Install Tempo
      kubernetes.core.helm:
        kubeconfig: "{{ kubeconfig_path }}"
        name: tempo
        chart_ref: grafana/tempo
        release_namespace: observability
        values:
          grafana:
            enabled: false

    - name: Install MLflow
      kubernetes.core.helm:
        kubeconfig: "{{ kubeconfig_path }}"
        name: mlflow
        chart_ref: community-charts/mlflow
        release_namespace: mlflow
        create_namespace: true
        values:
          defaultArtifactRoot: "s3://my-mlflow-bucket"
