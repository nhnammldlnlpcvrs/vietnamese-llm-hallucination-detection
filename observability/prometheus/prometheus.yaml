# observability/prometheus/prometheus.yaml
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'hallucination-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:8000']