apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlflow-runtime.fullname" . }}
  labels:
    {{- include "mlflow-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mlflow-runtime.selectorLabels" . | nindent 4 }}
